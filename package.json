{
  "name": "test-now-next-app",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "test": "jest",
    "deploy:staging:ci": "now -A now.json --target=staging --token=$NOW_TOKEN",
    "deploy:prod:ci": "now -A prod.now.json --target=production --token=$NOW_TOKEN",
    "deploy:staging": "now -A now.json --target=staging",
    "deploy:prod": "now -A prod.now.json --target=production"
  },
  "dependencies": {
    "next": "^9.0.2",
    "now": "^16.1.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "styled-components": "^4.3.2"
  },
  "engines": {
    "node": "10.x"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.3.3",
    "@now/node": "^0.12.5",
    "@types/jest": "^24.0.18",
    "@types/styled-components": "^4.1.18",
    "babel-plugin-styled-components": "^1.10.6",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  }
}